version: 2

models:
  - name: time_spine
    description: Time spine for MetricFlow
    columns:
      - name: date_day
        granularity: day
    time_spine:
      standard_granularity_column: date_day
